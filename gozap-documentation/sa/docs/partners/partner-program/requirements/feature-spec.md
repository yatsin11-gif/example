# Постановочная документация

## Метаданные

- Инициатива: Партнёрская программа
- Домен: partners
- Фича: Partner Program (управление партнёрами + кабинет партнёра)
- Запросил: SA
- Статус: Draft
- Дата: 2026-06-30
- Репозитории в скоупе: `gozap-admin`, `gozap-backend`
- Основной ответственный: SA

## Связанные артефакты

### Дизайн

- Не определено

### Обсуждения

- Входные SA-заметки `gozap_story.csv`, раздел «Админка для парнеров», 2026-06-30.

### Сопутствующие документы

- Не определено

## Проблема

- У бизнеса нет единого инструмента, чтобы заводить владельцев точек (партнёров) и привязывать к ним станции.
- Партнёр не видит свою статистику (станции, аренды, вознаграждение, баланс) и не может получать автоматические выплаты.
- Администратор не может настроить процент вознаграждения партнёра (применяется к прибыли со всех его станций) и сверять то, что видит партнёр.

## Цель

- Дать администратору инструмент для создания партнёров, редактирования данных партнёра, привязки/отвязки станций и настройки процента вознаграждения партнёра, применяемого к прибыли со всех его станций.
- Дать партнёру личный кабинет со статистикой по своим станциям, балансом и автоматическими ежемесячными выплатами через Stripe.

## Не входит в текущую итерацию

- Ручной запрос выплаты партнёром (выплата только автоматическая, раз в месяц).
- Управление договорами/юридическими документами партнёра.
- Множественные банковские счета на одного партнёра.
- Поддержка нескольких валют.
- Уведомления/email-рассылки партнёру (зафиксировано как открытый вопрос ниже).

## Триггер и аудитория

- Точка входа / триггер: администратор создаёт партнёра в админке после офлайн-договорённости; партнёр получает логин/пароль и заходит в свой кабинет.
- Основная аудитория: администратор (создание и управление партнёрами), партнёр (просмотр своей статистики и выплат).
- Вторичная аудитория: супер-админ, которому нужно видеть то же, что видит партнёр, для контроля эффективности точек.

## Целевое поведение продукта

### Сценарий

1. Администратор заводит партнёра (логин, пароль, имя, телефон, процент вознаграждения) и привязывает к нему одну или несколько станций; процент вознаграждения задаётся один раз для партнёра и применяется к прибыли со всех его станций; при необходимости администратор может отредактировать данные партнёра, включая процент.
2. Партнёр логинится (с возможностью сброса пароля по SMS) и попадает в раздел, где видны только его данные.
3. Партнёр видит список своих станций и сводку за период: баланс, ближайшую выплату, число завершённых аренд, сумму вознаграждения, разбивку по станциям; может провалиться в конкретную станцию и увидеть список аренд.
4. Система начисляет долю партнёра в баланс по каждой завершённой аренде и раз в месяц автоматически переводит накопленную сумму на привязанный счёт партнёра через Stripe.

### Правила результата

- Партнёр должен видеть только данные своих станций, без доступа к чужим.
- Администратор и супер-админ должны видеть ту же статистику, что видит партнёр, для любой выбранной точки.
- Администратор должен иметь возможность отредактировать данные уже созданного партнёра (логин, имя, телефон).
- Баланс партнёра должен учитывать аренду только за период, когда станция была привязана к этому партнёру.
- Накопленный баланс должен автоматически выплачиваться раз в месяц без ручного запроса.
- Партнёр должен иметь возможность выгрузить CSV по заработку и выплатам.

## Бизнес-правила

- Процент вознаграждения задаётся один раз для партнёра (пользователя) и применяется к прибыли со всех привязанных к нему станций, включая значение 0%.
- Одна станция может быть привязана только к одному партнёру одновременно; отвязка/привязка не пересчитывает прошлые начисления задним числом.
- Доля партнёра рассчитывается системой от суммы каждой завершённой аренды по проценту партнёра и добавляется в баланс автоматически.
- Поиск в списке партнёров выполняется по логину с частичным совпадением.
- Список партнёров по умолчанию сортируется по дате добавления от новых к старым; администратор/суперадмин может переключить сортировку на логин (по возрастанию/убыванию) или на дату добавления (от новых к старым/от старых к новым).
- Для указания реквизитов выплат партнёр нажимает кнопку и переходит на сторону Stripe: там он регистрируется или авторизуется и вводит данные банковского счёта/карты напрямую в Stripe. Данные карты не вводятся и не хранятся в формах `gozap-admin`/`gozap-backend`.
- Историю начислений и историю выплат партнёр смотрит в своём профиле в Stripe; для просмотра истории начислений партнёр редиректится в Stripe.
- Автоматическая выплата накопленного баланса выполняется раз в месяц через Stripe.
- Если на момент плановой ежемесячной выплаты у партнёра не указан банковский счёт/карта, выплата откладывается без потери баланса: как только партнёр указывает счёт/карту, накопленный баланс выплачивается, и далее выплаты продолжаются по обычному ежемесячному расписанию.

## Ограничения и известные лимитации

- Продукт / UX: партнёру не нужно запрашивать выплату вручную — флоу полностью автоматический по расписанию.
- Биллинг / правовые ограничения: для выплат используется Stripe; реквизиты счёта/карты партнёр вводит непосредственно на стороне Stripe через редирект (регистрация/авторизация в Stripe), что указывает на hosted-флоу в духе Stripe Connect; точный Stripe-продукт и порядок верификации партнёра (KYC) выполняются полностью на стороне Stripe и не подтверждены детально на продуктовом уровне.
- Технические ограничения, уже подтвержденные на продуктовом уровне: сброс пароля партнёра выполняется через SMS на номер телефона (используется Twilio).
- Внешние зависимости: Stripe (выплаты партнёрам), Twilio (отправка SMS для сброса пароля).

## Влияние на аналитику, CRM и коммуникации

- Аналитика: Не определено.
- CRM / подписочные системы: Не определено.
- Письма / уведомления: партнёр получает SMS при сбросе пароля (Twilio); уведомление о завершённой выплате не подтверждено — открытый вопрос.

## Пограничные сценарии

- Администратор отвязывает станцию от партнёра в середине периода — нужно сохранить начисления за период привязки без пересчёта.
- Партнёр не указал банковский счёт/карту к моменту ежемесячной автоматической выплаты — выплата откладывается и выполняется сразу после того, как партнёр пройдёт по кнопке в Stripe и укажет счёт/карту там, далее по обычному расписанию.
- Партнёр пытается открыть историю начислений в `gozap-admin` — система редиректит его в Stripe, где доступна и история начислений, и история выплат.
- У партнёра несколько станций — единый процент партнёра применяется к прибыли каждой из них, включая случай процента 0%.
- Поиск в списке партнёров по подстроке логина, не совпадающей ни с одним существующим логином, — список пуст.
- Автоматическая выплата через Stripe завершается ошибкой.
- Партнёр запрашивает сброс пароля, но номер телефона недоступен/не подтверждён.

## История изменений

### 2026-06-30

- Создан первый draft feature spec на основе SA-заметок по партнёрской программе (`gozap_story.csv`).
- Зафиксированы роли администратора и партнёра, привязка станций с процентом по каждой, расчёт баланса по периоду привязки, автоматическая ежемесячная выплата через Stripe и сброс пароля через Twilio SMS.

### 2026-06-30 (уточнение)

- SA уточнил состав раздела «Партнёры» в ролевой модели админки (`sa/docs/admin/admin-roles`): помимо создания, списка и информации о партнёре, раздел также включает редактирование данных уже созданного партнёра — добавлено в scope.

### 2026-06-30 (уточнение 2)

- SA подтвердил поведение ежемесячной выплаты при отсутствии счёта/карты: выплата не теряется и не пропускается окончательно, а откладывается. Как только партнёр указывает счёт/карту, накопленный баланс выплачивается, и далее выплаты продолжаются по обычному ежемесячному расписанию — закрывает прежний открытый вопрос.

### 2026-06-30 (уточнение 3)

- SA подтвердил флоу указания реквизитов выплат: партнёр нажимает кнопку и переходит на сторону Stripe, где регистрируется или авторизуется и вводит данные банковского счёта/карты; в `gozap-admin`/`gozap-backend` своей формы и хранения данных карты нет.
- SA подтвердил, что в своём профиле в Stripe партнёр видит и историю начислений, и историю выплат; для просмотра истории начислений партнёр редиректится в Stripe — закрывает часть прежнего открытого вопроса о точном Stripe-продукте (hosted-флоу регистрации/авторизации указывает на Connect-подобную модель).

### 2026-06-30 (уточнение 4)

- SA уточнил модель процента вознаграждения: процент задаётся не по каждой станции отдельно, а один раз для партнёра (пользователя) и применяется к прибыли со всех его станций — заменяет прежнюю модель «процент по станции».
- SA подтвердил поведение поиска в списке партнёров: поиск выполняется по логину с частичным совпадением.
- SA подтвердил поведение сортировки списка партнёров: по умолчанию — по дате добавления от новых к старым; администратор/суперадмин может переключить сортировку по логину (по возрастанию/убыванию) и по дате добавления (от новых к старым/от старых к новым).

## Открытые вопросы

- Какой именно продукт линейки Stripe Connect используется (Standard/Express/Custom) и какой объём верификации (KYC) Stripe требует от партнёра при регистрации — детали выполняются на стороне Stripe и не подтверждены продуктом.
- Нужны ли email/SMS уведомления партнёру о начислении и о выплате?
- Какой именно набор уровней доступа разделяет «супер-админа», который видит всё, и обычного администратора, управляющего партнёрами (см. `sa/docs/admin/admin-roles`)?
- Применяется ли изменённый администратором процент вознаграждения партнёра только к будущим начислениям, или нужно фиксировать историю изменений процента во времени?

## Допущения

- Партнёрский кабинет и админка для создания партнёров — это один и тот же веб-продукт `gozap-admin`, в котором роль `partner` видит только свои данные.
- Один партнёр может владеть несколькими станциями, но одна станция привязана только к одному партнёру в любой момент времени.
- День ежемесячной выплаты фиксированный (например, 1-е число), точная дата не подтверждена и считается open question только для implementation, а не для product scope.

## Связанные следующие артефакты

- Критерии: `sa/docs/partners/partner-program/requirements/feature-criteria.md`
- FE: `gozap-admin`
- BE: `gozap-backend`
- Admin: В scope (является частью этой же фичи)
- Landing: Не требуется
- Связанные фичи: `sa/docs/admin/admin-roles` (управляет видимостью раздела «Партнёры»)
