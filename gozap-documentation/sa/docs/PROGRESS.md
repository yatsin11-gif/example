# Progress — gozap-documentation

Единый трекер прогресса по фичам конвейера. Источник правды о том, что в работе, что
готово и что заблокировано — **без захода в Jira**. Детали этапов: [SYSTEM-MAP.md](./SYSTEM-MAP.md).

## Легенда статуса

- 🟢 **done** — этап пройден / артефакт утверждён
- 🟡 **in-progress** — в работе
- 🔴 **blocked** — ждёт ответа на open questions
- ⚪ **not-started** — ещё не начато

## Ростер фич

| Домен | Фича | Статус | Текущий этап | Что дальше | Open questions |
|---|---|---|---|---|---|
| partners | partner-program | 🟡 | QA-ready (9 test cases) | Qase sync (этап 5.5) | Какой именно Stripe-продукт используется? Нужна ли верификация (KYC)? Что происходит при отсутствии счёта/карты к моменту выплаты? |
| admin | admin-roles | 🟡 | QA-ready (5 test cases) | Qase sync (этап 5.5) | Точный список уровней доступа и разделов? Редактирование уровня доступа существующего администратора? Поведение при обращении к недоступному разделу (403/редирект/скрытие)? |
| kiosk | terminal-rental-flow | 🟡 | QA-ready (6 test cases) | Qase sync (этап 5.5) | Какой платёжный провайдер для карты на терминале станции? Как происходит окончание аренды и возврат powerbank? Поведение при ошибке SMS-кода/отклонении оплаты? |
| auth | sms-autofill | 🟡 | QA-ready (3 test cases) | Qase sync (этап 5.5) | Технический механизм получения SMS на станции? Нужен ли явный резервный сценарий ручного ввода? Авто-сабмит формы после автоподстановки? |

**Итог:** 4 фичи доведены до Jira-ready и QA test cases · ни одна не отправлена в Qase sync · реализация не начата.

## Сквозные пункты (актуальны всегда)

- **Repository naming — допущение.** Названия репозиториев (`gozap-admin`, `gozap-kiosk-app`,
  `gozap-backend`) не были явно заданы во входном CSV и приняты как рабочее допущение для
  Jira-ready спек. Уточнить у SA перед реализацией.
- **Stripe** подтверждён только для выплат партнёрам; точный продукт (Connect/Payouts) и
  необходимость KYC — не подтверждены.
- **Twilio** подтверждён как канал для всех SMS-сценариев (OTP при аренде, сброс пароля
  партнёра, уведомление о начале аренды).
- **Платёжный провайдер терминала станции** (оплата картой при аренде) не определён и
  отдельен от Stripe — открытый вопрос для `kiosk/terminal-rental-flow`.
- **Qase sync ещё не запускался** — нет подтверждённого Qase project/suite на вход (см.
  `qase.sync_blockers` в каждом `qa/test-matrix.yaml`).
- **Реализация** пойдёт в целевых репозиториях (`gozap-admin`, `gozap-kiosk-app`,
  `gozap-backend`); их прогресс сюда не подтягивается автоматически.

## Update log

- 2026-06-30 — репозиторий создан. Снимок состояния: 4 фичи (partners/partner-program,
  admin/admin-roles, kiosk/terminal-rental-flow, auth/sms-autofill) доведены через этапы
  1–5 (Spec → Criteria → Tasks → Jira → QA test cases) на основе `gozap_story.csv`. Конвейер
  и шаблоны повторяют `pdfbox-documentation-docs-human-readable-maps` без изменения файлов
  агентов. Следующий шаг для всех фич — Qase sync (5.5).

---

> Правила ведения: после закрытия этапа обнови строку в ростере, синхронизируй
> [SYSTEM-MAP.md](./SYSTEM-MAP.md) и добавь запись в update log с датой и сутью изменения.
